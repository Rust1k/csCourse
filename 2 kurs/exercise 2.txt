
Задача социальная сеть:

1. Добавить собственную валидацию данных:
	а) описать общий родительский класс валидации (метод IsValid, текст ошибки);
	б) создать классы аттрибутов валидации (например, проверка на пустоту; добавьте ещё один, например, использующий регулярки для проверки названия поста)
	в) предусмотреть указание текста ошибки, который должен выводиться при непрохождении валидации
2. Для соответствующих полей добавить аттрибуты. В случае непрохождения валидации выводить сообщения об ошибках с отображением той же формы.
3. Валидацию следует организовать вызовом соотвествующего метода IsValid аттрибута, используя рефлексию